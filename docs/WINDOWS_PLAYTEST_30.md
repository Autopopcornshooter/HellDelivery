# Windows 플레이 테스트 30 — 완료 후 함께 대기실로

2026-09-11-villa-30 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

배송 완료 화면에 호스트 전용 '함께 대기실로 · 캐릭터 변경 / 다시 준비' 버튼을 추가했다. 두 사람의 ENet 연결과 캐릭터 선택을 유지하고 완료한 레벨을 제거한다. 준비는 둘 다 해제하며 기존 로비 선택/준비/시작 흐름으로 새 배송을 시작한다. 바로 재시작과 연결 종료도 유지한다. 진행 중에는 복귀 버튼을 표시하지 않는다.

OnlineSession의 권위 RPC가 양쪽 상태 전환을 처리한다. 라운드 세대를 증가시켜 지난 배송의 스냅샷/완료/복구 응답이 새 로비에 영향을 주지 않게 한다. 입력·미리보기·월드 패킷 버퍼와 완료 메뉴를 정리한다. 프로토콜 30, 양쪽 동일 빌드 필요. 물리·맵·Input Map·Autoload 변경 없음. 기존 미커밋 작업과 빌드 보존, commit/push 없음.

## 검증과 확인 방법

Windows 두 프로세스로 기존 운반/복구/배송 완료 후 대기실 복귀, peer 유지, 준비 초기화, 이전 세대 완료 무시, 캐릭터 재선택/준비 후 새 배송, 즉시 재시작/재접속/종료를 검사한다. 202호는 합성 입력으로 계단 운반하며 초기 공동 잡기 배치와 마지막 201호 배송은 fixture다. 실제 사람 두 명의 플레이와 구분한다.

플레이 시에는 완료 화면 → 호스트 함께 대기실로 → 서로 캐릭터 변경 → 각자 준비 → 호스트 배송 시작 순서로 확인하면 된다. 일반 플레이 버그는 사용자 요청에 따라 다음 일괄 수정으로 남긴다. 실제 두 PC 전체 배송, 인터넷 지연/손실/장시간은 미검증이다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 기본 포트를 공유하므로 순차 실행한다.


## 최종 검증
Windows export 성공. NetworkFinal host 54/54 + client 36/36, BuildVisual 171/171: 총 261개 자동 검사 통과. 최종 로그 오류/경고 없음. UTF-8 128개와 git diff --check 통과. 완료 버튼과 복귀 대기실 캡처 직접 검토. 실제 두 PC 사람 플레이/인터넷 환경 검증은 미실행.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-30-online.zip, 44112003 bytes. SHA-256 BDC795CA520CC310E61A7C29F9241BE9610AE02F07FCD01900E7AEB157D62B30
압축 해제본 53 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
