# Windows 플레이 테스트 24 — 협동 기준 빌드 안정화

2026-09-11-villa-24 · Windows x86_64 · Godot 4.7.1 debug export

사용자가 빌드 23 오디오에 문제없다고 확인했다. 이번 작업은 현재 빌라 로컬 협동을 기준 빌드로 고정하기 위한 반복 인수 검사다. 새 콘텐츠/게임 규칙 변경은 없다. 미커밋 변경/이전 빌드 보존, commit/push/공개 배포 없음.

## 추가 검사

12회 반복, 매회:
- 두 사람 선택 후 배송 0과 패드 구독 1개 확인.
- 같은 상자에 두 사람 연결 후 일시정지 메뉴 복구: 양쪽 잡기와 공동 운반 표시가 모두 해제되는지 확인.
- 201→202와 202→201 순서를 교대하며 첫 배송 후 복구가 진행을 유지하는지 확인.
- 두 집 완료, 결과 화면, 재시작 후 선택/배송 초기화 확인.
- 이전 월드·P2·P2 오디오의 실제 해제와 메뉴 복귀 후 패드 신호 구독 제거 확인.

반복 검사에는 택배를 배송 구역에 배치하는 fixture가 사용된다. 이것은 12회 실제 운반 플레이가 아니다. 별도의 기존 P2 합성 패드 입력 계단 운반 경로와 전체 회귀도 실행한다. 수동 플레이·실물 패드 분리/재연결·장시간/다른 PC 검증과 구분한다.

## 사용자 확인/남은 범위

빌드 23에서 확인한 협동 오디오와 모션을 그대로 유지한다. 원하면 두 집을 완료하고 재시작을 몇 번 반복해 선택·배송·소리 중복 여부를 확인할 수 있다. 필수 재검증 요청을 위해 새 기능을 만든 빌드는 아니다.

현재 맵/로컬 협동 기준선을 보존하고 새 버그 피드백을 반영한다. 새 맵/운전/온라인은 별도 범위이며 이번에 구현하지 않았다.
재현: tools/Run-Godot.ps1 -Mode Export / BuildCoopVisual / BuildVisual.
검사 개발 이력: 최초 실행은 새 WeakRef 변수 타입 추론 경고가 오류로 처리되어 시작하지 못했다. 타입을 명시한 뒤 재내보내기/재실행했다. 최초 실패 로그는 validation/villa-24/InitialParse.log에 보존한다.

## 최종 검증
Windows export 성공. BuildCoopVisual 151/151(기존 55+반복 96), BuildVisual 167/167: 총 318개 자동 검사 통과. 최종 Godot 오류/경고 없음. 반복 12회 모두 완료. UTF-8 116개/ git diff --check 통과. 38-coop-completion.png 직접 검토: 공통 두 집 결과/재시작/메뉴 버튼 정상 렌더. 추가 제품 결함은 발견되지 않아 플레이 코드 변경 없이 빌드 23 동작을 유지한다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-24.zip, 43368109 bytes. SHA-256 215BC8965AAA126616E8F3991822940B35764CFF969A5001C1B799CD7E8B5FDE
압축 해제본 46 파일 해시 일치. 해제본 HellDelivery.exe headless 180프레임 exit 0, 오류/경고 없음. README/VALIDATION/라이선스/최종 로그/캡처 포함.
