# Windows 플레이 테스트 29 — 온라인 캐릭터·준비 대기실

2026-09-11-villa-29 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

사용자 요청에 따라 일반 플레이 버그 수정은 후속으로 보류하고 로비를 추가했다.

- 방 생성 후 호스트 캐릭터와 빈 참가자 자리를 표시한다. 참가하면 두 캐릭터가 함께 보인다.
- 기존 Kenney 18종 캐릭터를 실제 3D 미리보기로 재사용한다. 각자 화살표로 자기 캐릭터를 변경하고 기존 설정에 저장한다. 같은 캐릭터도 선택 가능하다.
- 각자 준비하기/준비 취소. 캐릭터를 바꾸면 그 사람의 준비만 해제한다.
- 두 사람 준비 완료 시에만 호스트의 배송 시작 버튼이 활성화된다. 서버에서도 조건을 검사한다.
- 접속만으로 레벨을 만들지 않는다. 호스트가 시작하면 선택한 두 캐릭터로 기존 온라인 빌라를 로드한다.
- 대기 취소/메뉴 복귀는 연결을 종료한다. 연결 종료 후 새 방에서는 준비가 초기화된다. 배송 중/완료 후 기존 호스트 재시작은 즉시 재시작한다.
- OnlineLobby는 미리보기/UI만 담당하고 OnlineSession이 로비 상태와 RPC를 소유한다. 미리보기마다 독립 3D 월드를 사용한다. Input Map/Autoload/운반 물리 변경 없음.
- 프로토콜 29: 양쪽 모두 이 빌드를 사용해야 한다. 기존 빌드와 미커밋 작업 보존. commit/push/공개 배포 없음.

## 검증 범위

실제 Windows 두 프로세스에서 로비 대기, 준비 전 시작 거부, 캐릭터 변경/준비 동기화, 준비 취소, 캐릭터 변경 후 준비 해제, 선택 모델의 레벨 적용, 배송·복구·재시작·재접속을 자동 검사한다. 실제 사람 두 명의 플레이와 구분한다.

202호는 네트워크 합성 입력으로 계단을 운반한다. 공동 잡기 초기 배치와 마지막 201호 배송은 fixture이다. 화면 캡처로 모델 방향/한글/버튼 배치를 별도 확인한다.

## 플레이 확인 목록과 다음 작업

1. 같은 빌드로 방 만들기/IP 참가 후 서로의 캐릭터가 보이는지 확인.
2. 내 캐릭터를 바꿨을 때 상대 화면에 반영되는지 확인.
3. 준비하기/취소, 준비 후 캐릭터 변경으로 시작 버튼이 잠기는지 확인.
4. 두 사람 준비 후 호스트 배송 시작, 선택한 모델이 적용되는지 확인.

일반 플레이 버그는 사용자가 나중에 모아서 전달할 예정이며 이번 완료 범위에 포함하지 않는다. 실제 두 PC 전체 배송/인터넷 지연·손실/장시간은 미검증. 호스트 이전, Steam 중계/초대, 채팅은 없다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 두 테스트는 기본 UDP 포트를 사용하므로 순차 실행한다.


## 최종 검증

Windows export 성공. NetworkFinal host 41/41 + client 32/32, 순차 재실행 BuildVisual 171/171: 총 244개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 127개 및 git diff --check 통과. 호스트 혼자 대기/두 사람 준비/참가자 준비 화면을 직접 검토해 캐릭터 방향, 한글, 버튼 화면 내 배치를 확인했다.

초기 BuildVisual은 동시에 실행한 온라인 검사의 UDP 포트 점유로 방 생성 검사 1개가 실패했다. 온라인 종료 후 같은 빌드 순차 재실행으로 171개 모두 통과했다. 첫 실패 로그는 validation/villa-29/PortConflictRun에 보존했다. 전달본에는 최종 성공 실행 자료를 포함한다. 실제 두 PC 사람 플레이/인터넷 지연 테스트를 통과한 것으로 기록하지 않는다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-29-online.zip, 44050168 bytes. SHA-256 E99D5EF1E37A910863C732FD1B25DE4E2EA656453865056776634D533302099D
압축 해제본 51 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
