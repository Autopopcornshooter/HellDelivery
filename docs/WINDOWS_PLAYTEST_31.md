# Windows 플레이 테스트 31 — 로비 퇴장 후 방 유지

2026-09-11-villa-31 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

로비에서 참가자가 취소/퇴장하면 호스트까지 연결 화면으로 나가던 동작을 보완했다. 호스트의 ENet 서버와 선택 캐릭터를 유지하고 참가자 ID/미리보기/양쪽 준비 상태를 초기화한다. 준비와 시작 버튼을 잠그고 같은 방 IP/포트를 표시한다. 재참가자는 기존 방에서 캐릭터 선택과 준비를 새로 진행한다. 완료 후 돌아온 로비에도 같은 규칙을 적용한다.

OnlineSession._peer_left는 in_lobby일 때만 방을 보존한다. 배송 중 퇴장과 호스트 종료는 기존 연결 종료 흐름이다. 라운드 세대를 증가시켜 이전 참가자 세션과 구분한다. 프로토콜 31, 양쪽 동일 빌드 필요. 물리/맵/Input Map/Autoload 변경 없음. 이번 변경은 아직 commit/push하지 않았다.

## 검증과 플레이 확인

Windows 두 프로세스에서 양쪽 준비 → 참가자 취소 → 호스트 peer 유지/준비 초기화/빈 자리/시작 거부 → 같은 방 재참가 → 새 준비/배송을 자동 검사한다. 이어서 기존 공동 운반, 202호 합성 입력 계단 운반, 개인 복구, 완료, 대기실 복귀/재시작, 배송 중 종료/재접속을 검사한다. 초기 공동 잡기 배치와 마지막 201호 배송은 fixture이다.

플레이할 때 로비에서 준비한 참가자가 취소한 후 같은 IP로 다시 들어와 확인하면 된다. 호스트는 방 만들기를 다시 누를 필요가 없다. 일반 플레이 버그는 사용자 요청대로 후속 일괄 수정 대상이며 실제 두 사람 전체 배송, 인터넷 지연/손실/장시간 검증은 미실행이다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 기본 포트를 공유하므로 순차 실행한다.


## 최종 검증
Windows export 성공. NetworkFinal host 62/62 + client 40/40, BuildVisual 171/171: 총 273개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 129개와 git diff --check 통과. 참가자 퇴장 후 호스트 대기 화면 캡처를 직접 검토해 한글, 빈 자리, 비활성 준비/시작 버튼을 확인했다. 실제 두 PC 사람 플레이/인터넷 환경은 미검증이다.
초기 온라인 실행 요청은 export가 끝나기 전 실행 파일이 없어 시작하지 못했으며, export 성공 후 실행한 NetworkFinal 결과만 위 집계에 포함했다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-31-online.zip, 44224386 bytes. SHA-256 EFA1E7B86F97F1BDEFA6F98656057589346888C3F5274D97B1513436FBFF1326
압축 해제본 54 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
