# Windows 플레이 테스트 38 — 온라인 반복 안정성

2026-09-11-villa-38 · Windows x86_64 · Godot 4.7.1 debug export

## 변경과 범위
온라인 검사에 선택 가능한 network-repeat 반복 모드를 추가했다. Run-OnlineTest.ps1 -Visual -RepeatRounds 8 -RunTag NetworkRepeat로 재현한다. 완료 → 대기실 → 준비 취소·캐릭터 변경·준비 → 새 배송을 8회 검사한다. 배송 순서를 매번 바꾸고 설정/조작 안내 열기·닫기도 반복한다.

이전 월드·두 캐릭터·두 오디오 노드의 WeakRef 해제, 설정 신호 연결 수 유지, 메뉴 숨김, 배송 수 초기화와 선택 캐릭터 적용을 확인한다. 발견된 제품 결함은 없어 게임 로직은 빌드 37과 같고 프로토콜 37을 유지한다. 빌드 도구 기본 출력은 38로 갱신했다. commit/push 없음.

## 검증
Windows 두 프로세스의 NetworkRepeat: 호스트 214/214, 참가자 156/156 통과. 로그 오류/경고 없음. 8회 반복 배송은 상자를 목적지에 배치하는 fixture이며 8회 수동 운반 검증은 아니다. 기존 검사에 포함된 202호 운반은 합성 입력으로 실행했다. 재참가와 로딩 중 연결 종료도 포함한다.

반복 종료 후 실제 렌더링 캡처를 직접 검토했다. 새 배송 HUD 0/2, 두 목적지 택배와 동료 표식이 표시된다. 시각 캡처 검토와 자동 검사 결과는 사람의 실제 플레이 완료를 뜻하지 않는다. 외부 인터넷·고지연·장시간 스트레스는 미검증이다.

## 플레이 확인과 다음 작업
양쪽 같은 빌드로 접속하여 몇 차례 배송 완료 후 대기실에서 캐릭터를 바꾸고 재시작한다. 준비 상태, 배송 수 초기화, 설정/조작 안내 닫힘을 확인한다. 사용자 실플레이에서 모은 버그를 다음에 일괄 수정한다. 기존 build31의 사용자 실테스트 이슈 0 기록은 유지하며 이번 환경 전체에 확대 해석하지 않는다.

## 전달
ZIP의 전체 폴더를 풀고 HellDelivery.exe를 실행한다. README.md에 조작법, licenses에 에셋 라이선스, verification에 실행 로그·검사 보고서·캡처, SHA256SUMS.txt에 파일별 해시를 포함한다.

## 최종 회귀 검증
BuildVisual 184/184, 온라인 370/370으로 총 554개 자동 검사 통과. Windows export 성공, 최종 로그 오류/경고 없음. UTF-8 137개 파일과 git diff --check 통과.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-38-online.zip, 46677929 bytes. SHA-256 1537D18D3FC419F9052B3F04DE4F71DB3AA178B3F232D0EE9F0570605FEFD785
압축 해제본 65 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
