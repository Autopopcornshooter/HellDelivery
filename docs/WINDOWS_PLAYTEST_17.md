# Windows 플레이 테스트 17 — 일시정지·진행 유지 복구

2026-09-10-villa-17 · Windows x86_64 · Godot 4.7.1 debug export

사용자 villa-16 HUD 가독성 확인 완료. 현재 빌라의 중단·복구·완료 흐름을 마감했다.

## 변경

- Esc 일시정지 화면에 배송 수량과 집별 현황/현재 안내를 표시한다.
- 출발 위치로 복구 · 배송 진행 유지 버튼을 추가했다. 기존 F5 복구를 호출하므로 완료 배송/내역을 유지하고 플레이어와 남은 물건만 복구한다. 복구 후 플레이와 마우스 제어를 재개하고 결과를 알린다.
- 재시작 버튼은 처음부터 다시 시작 · 배송 초기화로 명확히 구분했다.
- 일시정지/설정/조작법 화면 뒤 HUD를 숨기고 재개하면 복원한다. 완료 화면에서도 HUD를 숨기며 다시 플레이는 새 HUD로 시작한다.
- 밝은 배경과의 겹침을 줄이도록 일시정지 배경을 조정하고 960×540에서 새 메뉴를 확인했다.
- DEMO_READINESS.md의 오래된 201호 확인 대기 문구 등을 현재 확인 상태와 남은 범위로 정리했다. 기존 미커밋 작업과 이전 빌드는 보존했다.

## 검증

Windows EXE/PCK 기준. BuildMultiVisual 35개 통과: 부분 배송→일시정지 현황→메뉴 복구→완료 배송 보존/물건 복구→마우스/HUD 복원→전체 완료→다시 플레이를 검사한다. 판정용 물건 배치 검사이며 실제 왕복 운반은 이번에 재실행하지 않았다. 물리/동선 무변경이며 villa-15 왕복 41개 기록을 참고한다.

34-pause-delivery-recovery.png에서 최소 창 크기의 메뉴·현황·버튼·HUD 겹침 없음, 28-multi-stop-completion.png에서 완료 화면 뒤 HUD가 없는 것을 직접 검토했다. 사용자 수동 플레이와 구분한다.

최종 공통 회귀·인코딩·압축 인수 결과는 아래에 기록한다.

## 전달 및 다음 작업

builds/HellDelivery-2026-09-10-villa-17.zip 전체 압축 해제 후 HellDelivery.exe 실행. 새 배송 노선 · 201호 + 202호 선택. 한 집 배송 후 Esc를 눌러 진행 유지 복구와 처음부터 재시작의 차이를 확인해 주세요. F5/R도 유지한다.

상태 [REVIEW]. 다음은 현재 빌라의 복구 메뉴 체감 및 남은 구체적인 맵 피드백 반영이다. 다른 PC/GPU·장시간 실행은 미검증이며 온라인 협동·운전·새 레벨은 후속 범위다. commit/push/공개 배포 없음.

재현: tools/Run-Godot.ps1 -Mode Export / BuildMultiVisual / BuildVisual. GPU 검사는 순차 실행한다.

최종 BuildVisual 126개 통과. Export 및 두 최종 실행 로그 오류·경고 없음. UTF-8 106개 및 git diff --check 통과.

압축 인수: 39 개 파일 해시 일치. 별도 압축 해제 EXE headless 메뉴 실행 exit 0, 오류·경고 없음. ZIP 41459202 bytes, SHA-256 47C524A9FA4D615802BAD30CE6460530162DB74BEA27C334C6C0BDB312DD74C6. 압축 후 기록은 저장소에만 추가한다.
