# Windows 테스트 빌드 42 — 실시간 배송 목록

2026-09-11. 미세 버그보다 게임 플레이 시스템과 플로우를 우선한다.

## 변경
온라인 배송 중 Tab을 누르고 있으면 오른쪽 배송 목록을 표시한다. 주문명과 실제 배송 수, 택배마다 목적지/번호/무게/배송 완료/운반 대기/나와 동료의 운반 상태/택배까지 직선 거리를 표시한다. 같은 호수의 여러 택배도 #1/#2로 구분한다. 목록을 열어도 이동과 잡기를 유지한다. 목적지까지 길찾기 거리는 아니다.

기존 택배와 권위 서버에서 복제된 운반 정보를 사용한다. 새 RPC나 별도 배송 카운터를 만들지 않는다. 메뉴·결과·로딩·연결 종료에서 표시와 누름 상태를 정리한다. 메뉴의 Tab 포커스 이동은 유지한다. 목록을 펼친 동안 겹치는 동료 표식은 숨긴다. 기존 에셋과 주문 구성을 유지한다.

프로토콜 42: 두 사람 모두 빌드 42 사용. commit/push 없음.

## 검증
최종 실행 결과는 아래에 기록한다. 두 Windows 프로세스의 주문 전용 자동 검사는 일반/다건/공동 주문, 공동 들기, 부분 결과/취소/다음 주문/재참가를 확인한다. 배송 목적지 이동은 fixture이며 전체 계단을 실제 운반하는 수동 플레이가 아니다. Tab 입력/해제, 메뉴 후 잔류 방지, 주문별 행 수, 공동 운반자, 부분 배송 후 복구의 목록 상태를 추가 검사한다.

기존 계단의 간헐 운반 해제는 미해결이며 이번 검증 통과에 포함하지 않는다. 외부망·실제 두 PC·장시간 플레이도 미검증이다.

## 플레이 확인
- 다건 배송에서 Tab으로 201호/202호 택배를 나누어 옮기고, 배송 후 완료 표시를 확인한다.
- 공동 운반 주문에서 서로 같은 택배를 잡았을 때 나와 동료가 함께 표시되는지 확인한다.
- 운반 중 Tab을 눌렀다 놓고 메뉴를 열었다 닫아 표시와 운반이 자연스러운지 확인한다.

다음은 다건/공동 운반 주문의 실제 전체 플레이와 역할 분담 경험 확인이다. 미세 버그는 모아서 후속 수정한다.

ZIP 전체 압축 해제 후 HellDelivery.exe 실행. README, 라이선스, 검증 로그·캡처, 파일 해시 포함.
## 최종 검증 결과
ManifestRelease 호스트 103/103, 참가자 73/73, BuildVisual 193/193 — 실행한 범위 총 369개 자동 검사 통과. 최종 로그 오류/경고 없음. 일반/다건 목록의 Windows 캡처 직접 검토. UTF-8 142개 파일 검사 및 git diff --check 통과. 외부망/전체 계단 운반/사람의 실제 플레이는 별도 미검증.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-42-online.zip, 46804789 bytes. SHA-256 025B5C428466440CB5072F07BF2312557A9CAF517EC2487D9A5FBE02497E8E33. 압축 해제본 59 파일 해시 일치. 해제 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
