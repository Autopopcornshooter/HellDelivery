# Windows 플레이 테스트 32 — 배송 준비 화면

2026-09-11-villa-32 · Windows x86_64 · Godot 4.7.1 debug export

사용자가 villa-31 실제 테스트 이슈 0을 보고했다. 테스트 환경·지속 시간은 명시하지 않았으므로 모든 네트워크 조건을 통과했다고 확대하지 않는다.

## 변경

- 배송 시작/재시작 시 준비 화면을 먼저 표시하고 다음 프레임에 레벨을 구성한다.
- 내 월드 준비와 상대 월드 준비를 구분하며 두 준비가 완료된 뒤 입력과 호스트 물리를 활성화한다. 상대의 준비 응답이 먼저 도착해도 보존한다.
- 대기 중 취소 버튼을 유지한다. 취소한 지연 작업은 레벨을 다시 만들지 않는다.
- 준비 응답을 30초 넘게 기다리면 월드와 연결을 정리하고 시간 초과 안내를 표시한다.
- 동기식 씬 생성 자체는 메인 스레드 작업이다. 이 작업이 OS/엔진 수준에서 멈춘 동안 취소나 타이머가 실행된다고 보장하지 않는다. 안내는 씬 구성 전과 상대 응답 대기에 적용된다.
- OnlineSession과 테스트만 변경하며 기존 물리/맵/Input Map/Autoload는 유지한다. 프로토콜 32, 양쪽 같은 빌드 필요. commit/push 없음.

## 검증·다음 확인

실행 파일 두 프로세스 검사에서 참가자의 네트워크 polling을 120프레임 지연시켜 호스트 준비 화면, 조작 차단, 취소 버튼, 응답 복귀 후 시작을 확인한다. 단일 실행 파일 회귀 검사에서는 지연 생성 직전 취소와 준비 타임스탬프 주입을 통한 시간 초과 분기를 검사한다. 실제 30초 대기/인터넷 장애 시험과 구분한다.

기존 로비 퇴장/재참가, 네트워크 공동 운반, 202호 합성 입력 계단 배송, 복구/완료/대기실 복귀/재시작/종료도 재검증한다. 초기 공동 잡기 배치와 마지막 201호 배송은 fixture다.

사용자는 시작 버튼 이후 준비 안내와 정상 게임 시작을 확인하면 된다. 일반 플레이 버그는 후속 일괄 수정 대상으로 유지한다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 포트를 공유하므로 순차 실행한다.

## 최종 검증
Windows export 성공. NetworkFinal host 66/66 + client 40/40, BuildVisual 175/175: 총 281개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 130개와 git diff --check 통과. 온라인 준비 대기 캡처 직접 검토. 사용자 villa-31 실테스트 이슈 0과 이번 자동 검증은 별도로 기록한다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-32-online.zip, 44826190 bytes. SHA-256 E055AF0532048B59B7F4E3BA828CB4F948E1A2066A146E6F8AA7548E2D55F938
압축 해제본 56 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
