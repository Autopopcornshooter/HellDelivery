# Windows 플레이 테스트 33 — 온라인 개인 설정

2026-09-11-villa-33 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

온라인 Esc 메뉴에서 설정 화면을 열어 창 모드/해상도/시야각/마우스 감도/음량/그림자를 조정한다. 기존 SettingsPanel과 GameSettings 저장·적용 경로를 재사용하며 내 PC에만 적용한다. 온라인 입력에 쓰지 않는 게임패드 감도/반전 항목은 온라인 인스턴스에서만 숨긴다.

설정 중 내 입력은 메뉴처럼 중지하지만 SceneTree를 pause하지 않아 상대 움직임과 통신이 계속된다. Esc는 설정 → 온라인 메뉴 → 플레이 순서로 닫는다. 설정 화면에 상대 플레이가 계속됨을 안내한다. 완료/재시작/대기실 복귀/연결 종료 때 설정 화면을 정리한다. 설정은 온라인 플레이/완료 메뉴에서 접근하며 접속·로비·로딩 화면에는 버튼을 표시하지 않는다.

OnlineSession에 UI만 연결하며 기존 싱글/분할 화면 설정은 변경하지 않는다. 프로토콜 33, 양쪽 동일 빌드 필요. 새 Autoload/Input Map/맵/물리 변경 없음. commit/push 없음.

## 검증·플레이 확인

두 Windows 프로세스에서 참가자 FOV/감도/음량 변경, 실제 카메라 적용, 호스트 설정 불변, 설정 중 상대 위치 수신, Esc 단계 복귀/재개를 확인한다. 호스트 설정 중 참가자 움직임이 계속되는지, 참가자 설정 중 완료/호스트 종료가 설정을 닫는지도 검사한다. 테스트에서 변경한 설정은 원래 값으로 되돌린다.

기존 공동 운반/202호 합성 입력 계단 배송/복구/완료/로비 복귀/재시작/접속 취소/시간 초과도 재검증한다. 초기 공동 잡기 배치와 마지막 201호 배송은 fixture이며 실제 두 사람 플레이와 구분한다. 사용자 villa-31 실테스트 이슈 0 기록은 유지한다. 일반 플레이 버그는 후속 일괄 수정 대상이다.

확인 순서: 온라인 시작 → Esc → 설정에서 감도/시야각/음량 조절 → Esc → 계속하기. 상대가 움직이는 동안 설정을 열어 동작을 확인할 수 있다. 새 빌드의 실제 두 PC 설정 체감/전체 화면 전환은 사용자 확인 대상으로 남긴다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 포트를 공유하므로 순차 실행한다.

## 최종 검증
Windows export 성공. NetworkFinal host 72/72 + client 47/47, BuildVisual 175/175: 총 294개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 131개와 git diff --check 통과. 온라인 참가자 설정 캡처 직접 검토. 실제 두 PC 새 설정 체감은 사용자 확인 대상으로 남긴다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-33-online.zip, 44843903 bytes. SHA-256 BB0055522DA842222E353E94FFF65B4C38963C0C967862719C41E3B1D637C622
압축 해제본 57 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
