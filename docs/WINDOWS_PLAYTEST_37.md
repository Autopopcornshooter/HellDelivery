# Windows 플레이 테스트 37 — 접속 주소 편의

2026-09-11-villa-37 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

- 온라인 접속 화면에서 마지막 성공한 서버 IP와 최근 사용 포트를 복원한다. 참가자는 대기실 승인 후 저장하고 호스트는 방 생성 성공 시 포트를 저장한다.
- 주소 칸에서 IPv4:포트, [IPv6]:포트, 포트 없는 IP를 받는다. Enter로 참가하며 복합 주소의 포트는 포트 입력 칸에도 반영한다. 잘못된 IP/문자 포트/범위 밖 포트는 접속 전에 거부한다.
- 호스트 로비의 주소 선택 목록에서 LAN IPv4 또는 127.0.0.1 주소를 골라 클립보드에 복사한다. 여러 어댑터가 있으면 사용할 주소를 사용자가 선택한다. 복사는 버튼을 눌렀을 때만 실행한다. 로딩/플레이 중에는 숨긴다.
- GameSettings의 기존 settings.cfg에 network 필드를 추가한다. 손상된 IP/포트는 기본값 127.0.0.1/27926으로 복원한다. 화면·입력·음량 기본값 복원은 최근 서버 정보를 지우지 않는다.

프로토콜 37, 양쪽 같은 빌드 필요. 새 Autoload/물리/맵/Input Map 변경 없음. 클립보드는 텍스트만 복사하며 다른 사람에게 자동 전송하지 않는다. LAN 주소 공유 편의이며 인터넷 중계/공인 주소 탐색은 없다. commit/push 없음.

## 검증·플레이 확인

두 Windows 프로세스에서 IP:포트와 Enter 제출로 실제 대기실 참가, 주소 정규화와 성공 후 기억을 검사한다. 회귀 검사에서 IPv4/IPv6 문자열 해석, 잘못된 포트 거부, 설정 파일 저장/재로드/손상값 복원, 실제 Windows 클립보드 복사를 확인한다. 클립보드 테스트 후 기존 텍스트를 복원한다. IPv6는 파서 검증이며 실제 IPv6 네트워크 접속 검증은 아니다.

기존 로비/배송/복구/설정/동료 위치/끊김 후 재참가 흐름도 재검증한다. 기존 202호 운반은 합성 입력이며 초기 공동 잡기와 마지막 201호 배송은 fixture다. 실제 새 접속 편의 흐름과 다중 어댑터 선택은 사용자 확인 대상으로 남긴다.

확인: 호스트 방 만들기 → 주소 선택/복사 → 참가자 주소 칸 붙여넣기/Enter → 대기실. 다음 실행에서 주소와 포트가 남는지 확인한다. 일반 플레이 버그는 후속 일괄 수정 대상이다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 포트를 공유하므로 순차 실행한다.

## 최종 검증
Windows export 성공. NetworkFinal host 94/94 + client 60/60, BuildVisual 184/184: 총 338개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 136개와 git diff --check 통과. 주소 선택/복사 결과 캡처 직접 검토. 실제 IPv6 접속과 다중 어댑터 환경은 미검증이다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-37-online.zip, 46265522 bytes. SHA-256 F31EC617F02C7A4CADBC3DBEB5C6834B944C3210C68241E5C955C0A7291065E6
압축 해제본 64 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
