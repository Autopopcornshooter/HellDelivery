# Windows 플레이 테스트 36 — 배송 중 끊김 후 방 유지

2026-09-11-villa-36 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

참가자가 배송/로딩 중 연결을 종료하면 호스트의 ENet 서버를 닫던 흐름을 대기실 복귀로 통일했다. 같은 IP/포트에서 재참가를 받고 호스트의 캐릭터 선택을 유지한다. 중단된 월드/복구 요청/스냅샷/로딩 상태를 정리하고 양쪽 준비를 초기화한다. 재참가 후에는 처음부터 새 배송을 시작한다는 안내를 표시한다. 중단된 배송 진행을 이어받는 기능은 아니다.

_peer_left가 로비 외 상태에서는 _return_lobby를 로컬로 호출해 정리한다. 끊긴 상대에게 RPC를 보내지 않는다. 세대 증가와 loading=false가 지연된 로딩 작업/응답을 무효화한다. 동료 표식도 즉시 숨긴다. 호스트가 종료하거나 연결을 취소하면 기존처럼 방을 닫는다. 응답 없는 로딩의 30초 시간 초과는 기존 연결 종료 처리다.

프로토콜 36, 양쪽 동일 빌드 필요. 물리/맵/입력/Autoload 변경 없음. commit/push 없음.

## 검증·플레이 확인

Windows 두 프로세스로 배송 중 참가자 종료 → 호스트 peer 유지/월드 정리/초기화 안내 → 같은 방 재참가/새 배송을 확인한다. 추가로 참가자 polling을 지연한 채 로딩 중 연결을 종료해 호스트의 로딩 취소, 월드 잔류 없음, 같은 방 재참가를 검사한다. 기존 배송/복구/설정/조작법/동료 표시/로비/로딩 회귀도 실행한다.

202호 운반은 합성 입력이며 초기 공동 잡기 배치와 마지막 201호 배송은 fixture다. 실제 인터넷 장애/장시간 테스트와 구분한다. 사용자 villa-31 실테스트 이슈 0 기록은 유지한다.

사용자 확인: 배송 도중 참가자가 메뉴로 나가기 → 호스트 대기실의 배송 초기화 안내 → 같은 IP로 다시 참가 → 각자 준비 → 새 배송. 일반 플레이 버그는 후속 일괄 수정 대상이다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 포트를 공유하므로 순차 실행한다.

## 최종 검증
Windows export 성공. NetworkFinal host 94/94 + client 59/59, BuildVisual 175/175: 총 328개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 135개와 git diff --check 통과. 배송/로딩 중 연결 종료 뒤 대기실 캡처 직접 검토. 실제 인터넷 장애/장시간 검증은 사용자 확인 대상으로 남긴다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-36-online.zip, 46178456 bytes. SHA-256 56611FD3E62B5279DAC8C369CB85139D54EE98C02219C27EEC44EA6816D625A2
압축 해제본 63 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
