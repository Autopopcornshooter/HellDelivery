# Windows 플레이 테스트 35 — 동료 위치·거리 표시

2026-09-11-villa-35 · Windows x86_64 · Godot 4.7.1 debug export

## 변경

온라인에서 동료를 찾기 위한 작은 2D 표식을 추가했다. 화면 안에서는 캐릭터 위에 거리, 밖에서는 가장자리 화살표를 표시한다. 뒤쪽은 아래 방향으로 안내한다. 플레이어 높이 차가 2m를 넘으면 위쪽/아래쪽을 덧붙이며, 정확한 층수를 추정하지 않는다.

거리 2.5m 이내에서는 공동 운반 시야를 위해 숨긴다. 메뉴/설정/안내/로비/로딩/완료/연결 종료에서도 숨긴다. 상단 목표와 하단 조작 안내 영역을 피해 배치하고 텍스트 길이에 맞춰 패널 크기를 갱신한다. 표식은 클릭을 가로채지 않는다. 벽 너머 위치도 표시하는 동료 안내이며 경로 안내나 가시성 판정은 아니다.

OnlinePartnerMarker는 카메라와 두 위치로 표시만 계산한다. OnlineSession은 호스트 실제 플레이어/참가자 복제 플레이어 위치를 전달한다. 새 네트워크 메시지, 물리/맵/Input Map/Autoload 변경 없음. 프로토콜 35, 양쪽 동일 빌드 필요. commit/push 없음.

## 검증·플레이 확인

두 Windows 프로세스에서 동료를 앞/뒤/가까이 배치해 표시·화살표·숨김을 검사하고, 참가자가 기존 스냅샷으로 호스트 표식을 보는지 확인한다. 설정/완료/연결 종료 시 숨김도 검사한다. 기존 로비/조작 안내/설정/배송/복구/재시작/로딩 흐름을 재검증한다. 위치 배치는 fixture이며 202호는 합성 입력 계단 운반이다. 실제 두 사람 플레이와 구분한다.

사용자는 온라인에서 서로 떨어져 이동하며 거리/가장자리 방향과 가까운 거리 숨김을 확인하면 된다. 표시 위치·크기가 시야를 불편하게 하는지는 사용자 플레이 피드백 대상으로 남긴다. 일반 플레이 버그는 후속 일괄 수정 대상이다.

재현: tools/Run-OnlineTest.ps1 -Visual -RunTag <새 폴더>, tools/Run-Godot.ps1 -Mode BuildVisual. 포트를 공유하므로 순차 실행한다.

## 검증 실행 기록
초기 NetworkFinal의 참가자 표식 검사는 신뢰 채널의 단계 요청이 위치 스냅샷 반영보다 먼저 도착해 1개 실패했다. 테스트를 위치 반영 후 판정하도록 변경하고 배치 지점을 트럭 밖으로 옮겼다. 초기 로그를 보존하며 최종 전달에는 재검증 NetworkVerified 결과를 사용한다. 표시 거리는 이동 경로 길이가 아닌 두 캐릭터 사이 직선 거리다.

## 최종 검증
Windows export 성공. NetworkVerified host 79/79 + client 56/56, BuildVisual 175/175: 총 310개 자동 검사 통과. 최종 실행 로그 오류/경고 없음. UTF-8 134개와 git diff --check 통과. 앞쪽 캐릭터 위 표시, 뒤쪽 화살표, 참가자 표식 캡처 직접 검토. 높이 안내와 실제 플레이 가독성은 사용자 확인 대상으로 남긴다.

## 압축 후 전달 기록
ZIP HellDelivery-2026-09-11-villa-35-online.zip, 46086174 bytes. SHA-256 31F72591A8B69F962977AF426635BCCC862A169C5FF842D83C20402AFD7613D0
압축 해제본 62 파일 해시 일치. 해제본 실행 파일 headless 180프레임 exit 0, 오류/경고 없음.
