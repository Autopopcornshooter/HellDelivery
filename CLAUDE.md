# CLAUDE.md

이 문서는 이 저장소에서 Claude Code가 지켜야 할 작업 규칙을 정의한다.

## 1. 프로젝트 개요

- 엔진: Godot 4
- 스크립트: GDScript
- 장르: 3D, 3인칭, 물리 기반 협동 게임 (《택배기사 지옥》 / Delivery Hell)
- 목표 플랫폼: Windows PC, Steam 출시 목표
- 현재 최우선 목표: **싱글플레이 MVP-1**

게임 기획의 세부 내용은 `docs/GAME_DESIGN.md`를 기준으로 한다.

## 2. 역할

- 사용자가 최종 결정권자다.
- `docs/GAME_DESIGN.md`는 확정 기획 기준이다.
- Claude는 핵심 기획(장르, 코어 루프, MVP 범위, Phase 순서 등)을 임의로 변경하지 않는다.
- 새로운 아이디어나 `GAME_DESIGN.md`에 없는 기능이 필요하다고 판단되면, 바로 구현하지 않고 먼저 제안한다.

## 3. 작업 절차

### 2026-09-11 차량·전체 배송·평가 승인

사용자가 진행 현황의 3·4단계를 한 묶음으로 요청했다. 물류센터 적재→차량 탑승/운전/하차→빌라 배송, 차량 문과 물리 화물, 택배 충격/제한 시간/성공·실패·등급·점수, 온라인 동기화와 재도전까지 구현·검증·Windows 빌드를 연속 수행한다. 기존 차량/파손/평가 제외 규칙은 이 범위에 적용하지 않는다. 수치는 테스트용으로 문서화하고 기존 빌라 연습 모드는 유지한다. 유료 구매·영구 경제/성장·Steam·원격 push는 포함하지 않는다. 현재 미커밋 작업은 보존한다.

### 2026-09-11 빌라 기준선·온라인 4인 승인

사용자가 진행 현황의 1·2단계(빌라 협동 기준선 확인, 온라인 기반 완성)를 한꺼번에 진행하도록 요청했다. 현재 빌라 진행 방해 문제 해결, 온라인 2~4인 로비/운반/배송/복구/재접속, 로컬 다중 프로세스·화면 검증과 Windows 빌드를 연속 수행한다. 기존 2인 전용 범위 제한은 이 작업에 적용하지 않는다. 실제 여러 PC/외부망 검증은 직접 수행한 범위와 분리한다. 차량/경제/Steam 배포/원격 push는 포함하지 않는다.

### 2026-09-11 플레이 시스템·플로우 우선

사용자가 미세한 버그 수정보다 게임 플레이 시스템과 플로우 구현을 우선하라고 지시했다. 현재 빌라 온라인 2인 배송에서 일반/다건/공동 운반 주문 선택, 실제 택배·목표 반영, 배송 진행·시간·결과, 다음 주문 준비를 한 묶음으로 구현·검증·빌드한다. 진행을 막는 오류만 즉시 고치고 미세한 물리·시각 조정은 후순위로 기록한다. 기존 에셋과 기획의 주문·무게·배송 루프를 활용하며 차량·경제·공개 배포는 이번 묶음에 추가하지 않는다.

### 2026-09-11 온라인 2인 빌라 승인

사용자가 현재 빌라 온라인 2인 협동 제안(방 만들기/IP 참가, 캐릭터/택배/배송 동기화, 연결 종료/메뉴 복귀)에 "고고"로 승인했다. 이 범위의 구현/문서/로컬 두 프로세스 검증/Windows 빌드를 연속 수행한다. 이전 온라인 제외 규칙은 이 승인 범위에 적용하지 않는다. Steam 초대/중계, 차량, 원격 push/공개 배포는 제외한다.


### 2026-09-10 Windows 테스트 빌드 작업 예외 (최신 사용자 지시)

- 이번 목표는 기존 캐릭터로 대표 레벨 하나를 끝까지 플레이할 수 있는 Windows 테스트 빌드 전달이다.
- 목표에 필요한 코드·씬·문서 수정, 작업 브랜치 생성, 로컬 실행·검증·빌드를 연속 수행한다.
- 이 목표 안에서는 기능마다 사용자에게 "다음" 지시를 기다리는 규칙과 1회 1기능 제한을 적용하지 않는다.
- 핵심 기획 변경, 유료 구매, 원격 push, 공개 배포는 제외한다. 미커밋 변경은 보존한다.
- 자동 검증과 실제 플레이·시각 검증을 구분하고 미실행 항목을 통과로 기록하지 않는다.
- 현재 상태·다음 작업은 `docs/WINDOWS_PLAYTEST.md`를 우선 확인한다. 아래 Phase 0 표기는 과거 이력이다.

1. 요청받은 작업 전에 관련 문서(`docs/GAME_DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/TASKS.md`)를 읽는다.
2. 현재 프로젝트/코드 상태를 먼저 확인한다.
3. 구현 전에 설계, 변경 파일 목록, 영향 범위를 설명한다.
4. 사용자의 승인이 필요한 요청에서는 승인 전 구현하지 않는다.
5. 한 번에 하나의 기능만 작업한다.
6. 작업 후 변경 내용, 테스트 방법, 남은 문제를 요약한다.
7. 완료된 작업은 `docs/TASKS.md`에 반영한다.

## 4. 개발 원칙

- MVP 우선.
- 최소 구현 우선.
- 과도한 추상화 금지.
- 불필요한 Manager, Singleton, Factory 생성 금지.
- 기존 코드와 씬을 이유 없이 대규모 수정하지 않는다.
- Godot 4의 현재 권장 방식과 GDScript를 사용한다.
- 경고와 오류를 숨기지 않는다.
- 하드코딩이 필요한 프로토타입 값은 명확하게 표시한다 (예: `# TODO: 프로토타입 값, 튜닝 필요`).
- 한글이 있는 소스·문서는 UTF-8로 명시해서 읽고 쓴다. Windows PowerShell의 기본 Get-Content/Set-Content 인코딩에 의존하지 않는다. 빌드 전 tools/Validate-SourceText.ps1로 인코딩을 검사한다.
- 향후 확장 가능성만을 이유로 복잡하게 설계하지 않는다.

## 5. Godot 작업 규칙

- 씬과 스크립트의 책임을 분명히 한다.
- 노드 구조를 먼저 설명한다.
- Input Map 변경 사항을 명시한다.
- Autoload는 정말 필요한 경우에만 사용한다.
- 리소스 경로와 노드 경로를 불필요하게 깨뜨리지 않는다.
- 물리 처리는 가능한 한 `_physics_process`에서 처리한다.
- `CharacterBody3D`, `RigidBody3D` 등 노드 특성에 맞게 구현한다.
- 에디터에서 수동 작업이 필요하면 정확한 절차를 설명한다.

## 6. 현재 금지 범위

MVP-1 완료 전에는 아래 기능을 구현하지 않는다 (`docs/GAME_DESIGN.md` 섹션 27 "MVP 제외 기능" 기준).

- 온라인 멀티플레이
- 차량
- 파손 및 내구도
- 랜덤 이벤트
- 경제 시스템
- Steam 연동
- 저장 시스템
- NPC 및 고객
- 정식 로컬 분할 화면
- 여러 종류의 택배
- 일반 문 상호작용
- 좁은 문 테스트

이 목록은 `GAME_DESIGN.md`의 발췌본이다. 전체 목록과 각 항목이 해금되는 Phase는 `docs/GAME_DESIGN.md` 섹션 27, 29를 따른다.

## 7. 코드 품질

- 이름은 영어로 작성한다.
- 코드와 파일 이름은 일관된 규칙을 사용한다 (클래스/씬 파일: `PascalCase`, 변수/함수: `snake_case`).
- 주석은 코드 내용을 반복하지 말고 이유와 제약을 설명할 때만 사용한다.
- 함수는 한 가지 책임을 갖도록 한다.
- 매직 넘버는 export 변수 또는 상수로 관리한다.
- 불필요한 전역 상태를 만들지 않는다.
- 오류 가능성이 있는 부분은 방어적으로 처리한다.

## 8. Git 관련 규칙

- 요청 없이 commit, push, branch 생성, reset, rebase를 실행하지 않는다.
- 파일 삭제와 대규모 변경 전에 반드시 알린다.
- 생성 파일과 수정 파일을 작업 후 정리해서 보고한다.

## 9. 문서 우선순위

충돌이 있을 경우 다음 순서를 따른다.

1. 사용자의 최신 명시적 지시
2. `docs/GAME_DESIGN.md`
3. `CLAUDE.md`
4. `docs/ARCHITECTURE.md`
5. `docs/TASKS.md`
6. 기존 코드

## 10. 현재 개발 단계

- Phase 0 — 프로젝트 준비 단계.
- 다음 목표는 프로젝트 구조와 기술 설계 확정.
- 아직 게임 코드는 구현하지 않는다.


## 절대 하지 말 것

Claude는 다음 행동을 사용자의 명시적인 요청 없이 수행하지 않는다.

- 새로운 폴더 구조 생성
- 프로젝트 구조 대규모 변경
- 새로운 디자인 패턴 도입
- 외부 Asset 다운로드
- 외부 Plugin 설치
- Addon 추가
- Input Map 삭제
- Scene 이름 변경
- Script 이름 변경
- 기존 API 변경
- Save 방식 변경

구현 전에는 항상 다음 형식으로 보고한다.

## 작업 목표

...

## 수정 파일

...

## 생성 파일

...

## 설계

...

## 예상 영향

...

## 위험 요소

...

Godot에서는

- Node를 불필요하게 중첩하지 않는다.
- export 변수를 적극 사용한다.
- Inspector에서 수정 가능한 값은 export 한다.
- Scene을 재사용 가능하게 만든다.
- Autoload는 최소화한다.
- _ready()는 초기화만 수행한다.
- _physics_process()와 _process()를 혼용하지 않는다.

문서에 없는 기능을
'편의상'
'좋아 보이므로'
'확장성을 위해'

추가하지 않는다.

AI는
"많이 만드는 것"보다
"작게 만들고 자주 검증하는 것"을 우선한다.

한 번의 작업은
최대 1개의 기능만 구현한다.

사용자가
"다음"

이라고 말하기 전까지
다음 기능을 구현하지 않는다.
